## 成果物

- 正本HTML: `<repository-relative-path-with-YYYYMMDD-HHMMSS-mmm>`
- 完了時刻: `<local-completion-time>`
- 対象ユーザー: `<target-user>`
- 主要タスク: `<primary-task>`
- コンテキスト逆質問: `<asked N | skipped (reason)>`
- 推論した項目: `<none | context-items>`

## 画面と状態

- `<screen-or-state>`

## 検証結果

- 構造検証: `<pass | fail>`
- ブラウザ操作: `<pass | fail | not run (reason)>`
- 320px前後: `<pass | fail | not run (reason)>`
- 1280px前後: `<pass | fail | not run (reason)>`
- キーボード操作: `<pass | fail | not run (reason)>`
- 最終化後ランタイム: `<pass | fail | not run (reason)>`
- 静的HTML保存リンク: `<pass | fail | not run (reason)>`
- 静的HTML引継ぎ: `<pass | fail | not run (reason)>`
- サーバー停止後の表示: `<pass | fail | not run (reason)>`
- プレビューサーバー状態: `<stopped-and-reaped | intentional-handoff | not run (reason)>`
- プロセス識別子: `<PID | process handle | job ID | none>`
- 停止後の生存・URL確認: `<pass | fail | not run (reason)>`
- ホットリロード: `<pass | fail | not run (reason)>`
- 経過時間: `<pass | fail | not run (reason)>`
- 視覚差分: `<generated | not run (reason) | not applicable>`

## 実行コマンド

- `<command>`: `<result>`

## 前提

- `<assumption>`

## 未解決事項

- `<open-question-or-none>`

<!-- 改稿以外では、次の3セクションを要素ごと削除する -->

## 今回の変更

- `<change-id>: <change-summary>`

## 変更していない範囲

- `<unchanged-screen-or-state>`

## 再レビュー対象

- `<decision-id>: <decision>`
