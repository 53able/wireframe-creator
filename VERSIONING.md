# Agent Skillのバージョニング規範

この文書は、単独リポジトリで公開するAgent Skillのバージョン、タグ、GitHub Release、配布ZIPを一貫して管理するための規範です。対象読者は、スキルを変更し、互換性を判断し、リリースする保守担当者です。

## 1. 基本原則

1. バージョン番号には[Semantic Versioning 2.0.0](https://semver.org/spec/v2.0.0.html)の`MAJOR.MINOR.PATCH`形式を使います。
2. バージョンはコード量ではなく、利用者とエージェントから観測できる契約の変化で決めます。
3. `VERSION`、Gitタグ、GitHub Release、ZIP名のバージョンを一致させます。
4. リリースはGitHub Actionsの単一ワークフローから実行し、手作業でタグやReleaseを先に作りません。
5. 配布ZIPにはスキルの実行に必要なファイルだけを含めます。リポジトリ運用用の文書やCI設定は含めません。

この文書では、**必須**を例外なく守る条件、**推奨**を合理的な理由がない限り守る条件として扱います。

## 2. バージョンの正本

[Agent Skills仕様](https://agentskills.io/specification)の標準frontmatterには`version`フィールドがありません。任意の`metadata`へ独自バージョンを置くことはできますが、複数箇所の同期が必要になります。そのため、単独スキルリポジトリではルートの`VERSION`を唯一の正本とします。

| 対象 | 形式 | 例 |
|---|---|---|
| バージョンファイル | `MAJOR.MINOR.PATCH` | `1.4.2` |
| Gitタグ | `vMAJOR.MINOR.PATCH` | `v1.4.2` |
| Release名 | `<skill-name> vMAJOR.MINOR.PATCH` | `wireframe-creator v1.4.2` |
| ZIP名 | `<skill-name>-vMAJOR.MINOR.PATCH.zip` | `wireframe-creator-v1.4.2.zip` |
| ZIP内のルート | `<skill-name>/` | `wireframe-creator/` |

`SKILL.md`へ同じバージョンを重複記載しないことを推奨します。エージェントの判断にバージョンが必要な場合だけ、CIで`VERSION`から機械的に同期します。

## 3. どの番号を上げるか

### MAJOR

既存利用者が同じ入力や手順を使ったとき、従来の結果を期待できなくなる変更です。

- `name`またはスキルディレクトリ名を変更する
- 主要な起動条件を削除、反転、または別用途へ変更する
- 成果物の形式、保存場所、必須項目を互換性なく変更する
- 既存のコマンド、引数、スクリプト、テンプレートを削除または改名する
- 必須ランタイム、必須ツール、権限、ネットワーク条件を引き上げる
- 既定動作を変更し、従来のワークフローへ安全に戻せない

### MINOR

既存契約を保ったまま、利用可能な能力を追加する変更です。

- 新しい任意モード、状態、フローを追加する
- 新しい`references/`、`assets/`、`scripts/`を追加する
- 既存の入力を保ったまま、新しい入力形式を受け付ける
- 既存成果物を壊さず、新しい検証やエラー診断を追加する
- オプトインの外部連携を追加する

### PATCH

既存契約を変えず、誤りや曖昧さを修正する変更です。

- 誤字、リンク、説明不足を修正する
- 既存の指示どおりに動かないスクリプトを修正する
- 検証漏れや不正なエラー処理を直す
- 出力契約を変えない範囲で内部実装を整理する
- 既存挙動を変えないCI、テスト、配布処理を改善する

文章だけの変更でも、エージェントの選択や出力を変える場合はPATCHとは限りません。特に`description`、禁止事項、必須手順、成果物契約の変更は、観測される挙動を基準にMINORまたはMAJORと判定します。

## 4. 互換性の判断単位

変更前後を次の境界で比較します。

1. **発火契約**: どの依頼でスキルが選ばれ、どの依頼では選ばれないか
2. **入力契約**: 必須情報、対応ファイル、引数、前提条件
3. **処理契約**: 必須工程、確認ゲート、禁止操作
4. **出力契約**: ファイル形式、配置、命名、必須内容
5. **実行環境**: PythonやNode.jsのバージョン、外部ツール、ネットワーク、権限
6. **失敗契約**: 終了コード、エラー表示、復旧方法、副作用

内部構成が変わっても境界が同じならPATCH候補です。境界を追加するだけならMINOR候補、既存境界を壊すならMAJOR候補です。

## 5. 配布ZIPの契約

ClaudeなどへアップロードするZIPは、次の構造を必須とします。

```text
<skill-name>/
├── SKILL.md
├── LICENSE
├── VERSION
├── assets/
├── references/
└── scripts/
```

配布対象はallowlistで選びます。次のファイルは含めません。

- `.git/`、`.github/`
- `README.md`、`CHANGELOG.md`などのリポジトリ運用文書
- `dist/`、キャッシュ、一時ファイル
- テスト用データ、ローカルログ、秘密情報
- シンボリックリンク

ZIPは同じリビジョンとバージョンから同じバイト列を生成できるよう、ファイル順、タイムスタンプ、パーミッションを固定することを推奨します。公開前に展開テストを行い、`<skill-name>/SKILL.md`の存在と、トップレベル外のファイルがないことを確認します。

## 6. CIが所有するリリース手順

[`vercel-labs/skills`のPublishワークフロー](https://github.com/vercel-labs/skills/blob/main/.github/workflows/publish.yml)と同じく、`workflow_dispatch`から更新種別を選び、バージョン更新、タグ、公開を1つのジョブで処理します。このリポジトリではnpmパッケージの代わりにスキル単体ZIPを公開します。

1. `main`を履歴付きでcheckoutする。
2. `patch`、`minor`、`major`から更新種別を受け取る。
3. 現在の`VERSION`と同名のタグから`HEAD`までの変更を収集する。
4. `VERSION`を更新し、`CHANGELOG.md`とRelease notesを生成する。
5. スキル、リリーススクリプト、ワークフローを検証する。
6. allowlistから決定的なZIPを生成し、展開テストとSHA-256計算を行う。
7. `VERSION`と`CHANGELOG.md`をリリースコミットにまとめる。
8. 注釈付きタグを作成し、`main`とタグをatomic pushする。
9. 同じジョブでGitHub Releaseを作成し、ZIPを添付する。

GitHub Actionsの`GITHUB_TOKEN`で発生させたイベントは、原則として別のワークフロー実行を開始しません。この制約は[GitHubのワークフロートリガーの説明](https://docs.github.com/en/actions/how-tos/write-workflows/choose-when-workflows-run/trigger-a-workflow)に明記されています。タグ作成とZIP添付を別ワークフローへ分けず、同じリリースジョブに置くのはこのためです。

書き込み権限はリリースワークフローだけへ`contents: write`として付与します。長期PATを直接保存せず、まず`GITHUB_TOKEN`を使用します。branch protectionでbotのpushを禁止する場合は、環境承認または専用GitHub Appを使います。

## 7. リリース前の必須検証

- `SKILL.md`のfrontmatterがAgent Skills仕様へ適合する
- `name`とスキルディレクトリ名が一致する
- 参照している`assets/`、`references/`、`scripts/`が存在する
- 同梱スクリプトの構文検査とテストが通る
- ZIP内の`VERSION`がリリースバージョンと一致する
- ZIPが破損しておらず、トップレベルが1つのスキルフォルダだけである
- 不要ファイルと秘密情報が混入していない
- GitHub Releaseのタグ、名称、ZIP名が同じバージョンを示す

テスト未実施の項目を合格扱いにしません。ブラウザ操作や外部サービスが必要な検証を省略した場合は、Release notesまたは実行ログへ`not run`と理由を残します。

## 8. CHANGELOGとRelease notes

`CHANGELOG.md`はバージョン単位で、利用者に見える変更だけを記録します。コミットの羅列ではなく、発火条件、入力、出力、互換性への影響が分かる表現へ直すことを推奨します。

Release notesには次を含めます。

- 主要な変更
- 破壊的変更と移行方法
- CLIからのインストール方法
- 添付ZIPの用途
- 未実施の検証または既知の制約

ZIPの説明はRelease notesへ置き、READMEへリリース運用の説明を重複させません。

## 9. 廃止と移行

既存機能を直ちに削除できない場合は、少なくとも1つのMINORリリースで非推奨として告知します。

1. 旧機能を残したまま代替手段を追加する。
2. `CHANGELOG.md`とRelease notesへ廃止予定を記載する。
3. 移行方法と削除予定のMAJORバージョンを示す。
4. 次のMAJORリリースで旧機能を削除する。

セキュリティ上の理由で即時停止が必要な場合は、この猶予を設けなくてよいものとします。その場合も、影響範囲と代替手段をRelease notesへ明記します。

## 10. 失敗時の扱い

### タグのpush前

原因を修正し、同じ更新種別でワークフローを再実行します。リリースされていない番号を手作業で進めません。

### タグのpush後、Release作成前

新しいバージョンを作りません。既存タグから同じZIPを再生成し、そのタグに対するGitHub Releaseを復旧します。

### Release公開後

公開済みタグとZIPを差し替えません。修正は新しいPATCH以上のバージョンとして公開します。誤公開に秘密情報が含まれる場合は、Releaseと資産を直ちに非公開または削除し、認証情報を失効させた上でインシデントとして扱います。

## 11. リリース判定チェックリスト

- [ ] 変更はMAJOR、MINOR、PATCHのどれかに分類されている
- [ ] `main`へ対象変更が取り込まれている
- [ ] 前回タグからの変更内容を確認した
- [ ] 破壊的変更には移行手順がある
- [ ] 自動テストとスキル検証が通っている
- [ ] ZIPはスキル単体で、展開テスト済みである
- [ ] `VERSION`、タグ、Release、ZIP名が一致している
- [ ] Release notesに利用者へ必要な変更と制約がある
- [ ] 公開後にZIPを再ダウンロードし、破損と構造を確認した
